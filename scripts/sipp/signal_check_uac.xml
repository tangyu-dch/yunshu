<?xml version="1.0" encoding="UTF-8"?>
<!--
  SIP 信令通路检测 - SIPp UAC 场景

  用途: 验证 SIP 信令通路是否通畅（不依赖坐席注册）
  判定:
    - 收到 100 Trying = SIP 传输层正常
    - 收到 4xx/5xx/6xx = SIP 事务层正常（被叫不可达等业务拒绝）
    - 收到 200 OK = 完整通话建立成功
    - 超时无响应 = 信令不通
-->
<scenario name="signal_check_uac">
  <!-- 发起 INVITE -->
  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: [cseq] INVITE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: yunshu-signal-check
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 8 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-16
      a=sendrecv
    ]]>
  </send>

  <!-- 100 Trying (传输层确认) -->
  <recv response="100" optional="true" />

  <!-- 任意 1xx 临时响应 -->
  <recv response="180" optional="true" />
  <recv response="183" optional="true" />

  <!-- 成功: 200 OK -->
  <recv response="200" rtd="true">
    <action>
      <exec int_cmd="set_test" value="call_established" />
    </action>
  </recv>

  <!-- ACK -->
  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: [cseq] ACK
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- 通话 3 秒 -->
  <pause milliseconds="3000" />

  <!-- 挂机 -->
  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[call_number]
      To: [service] <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: [cseq] BYE
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <recv response="200" />

  <!-- 定义: 4xx/5xx/6xx 业务拒绝路径 -->
  <label name="rejected" />
  <recv response="404" optional="true" />
  <recv response="480" optional="true" />
  <recv response="486" optional="true" />
  <recv response="503" optional="true" />

  <timewait milliseconds="1000" />
</scenario>

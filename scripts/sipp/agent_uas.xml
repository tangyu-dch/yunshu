<?xml version="1.0" encoding="UTF-8"?>
<!--
  坐席模拟器 - SIPp UAS 场景

  用途: 模拟坐席 SIP 电话，接收系统 INVITE
  信令: INVITE → 100 → 180 Ringing → 200 OK(SDP) → ACK → 通话 → BYE → 200 OK

  使用方法:
    sipp -sf agent_uas.xml -s 1001 -i 192.168.107.0 -p 6060 -m 1 -nostdin 192.168.107.6:5060
-->
<scenario name="agent_uas">
  <!-- 收到 INVITE -->
  <recv request="INVITE" />

  <!-- 回复 100 Trying -->
  <send>
    <![CDATA[
      SIP/2.0 100 Trying
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Content-Length: 0
    ]]>
  </send>

  <!-- 模拟振铃 1 秒 -->
  <pause milliseconds="1000" />

  <!-- 回复 180 Ringing -->
  <send>
    <![CDATA[
      SIP/2.0 180 Ringing
      [last_Via:]
      [last_From:]
      [last_To:];tag=[call_number]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: sip:[service]@[local_ip]:[local_port]
      Content-Length: 0
    ]]>
  </send>

  <!-- 模拟摘机延迟 -->
  <pause milliseconds="500" />

  <!-- 回复 200 OK (摘机接听) -->
  <send>
    <![CDATA[
      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:];tag=[call_number]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: sip:[service]@[local_ip]:[local_port]
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 8 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-16
      a=sendrecv
    ]]>
  </send>

  <!-- 收到 ACK -->
  <recv request="ACK" />

  <!-- 模拟通话 5 秒 -->
  <pause milliseconds="5000" />

  <!-- 坐席主动挂机 -->
  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[local_ip]:[local_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: [service] <sip:[service]@[local_ip]:[local_port]>;tag=[call_number]
      To: sipp <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: [cseq] BYE
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- 收到 200 OK -->
  <recv response="200" />

  <!-- 定义: 收到对方 BYE (对方先挂) -->
  <label name="peer_bye" />
  <recv request="BYE" optional="true" />
  <send optional="true">
    <![CDATA[
      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Content-Length: 0
    ]]>
  </send>

  <timewait milliseconds="1000" />
</scenario>
